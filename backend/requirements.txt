# Dependências FastAPI + MySQL
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic[email]>=2.7.0
pydantic-settings>=2.3.0

# Banco de dados
sqlalchemy>=2.0.0
aiomysql>=0.2.0
alembic>=1.13.0

# Segurança
python-jose[cryptography]>=3.3.0
# bcrypt travado em 4.0.1: passlib 1.7.4 detecta o backend lendo
# bcrypt.__about__.__version__, removido no bcrypt>=4.1 — quebra o hash
# de senha em runtime (ValueError: password cannot be longer than 72 bytes).
passlib[bcrypt]>=1.7.4
bcrypt==4.0.1
python-multipart>=0.0.9

# Rate limiting (proteção contra força bruta no login)
slowapi>=0.1.9

# Utilitários
python-dotenv>=1.0.0
aiosmtplib>=3.0.0
